version: '3'
services:
  web:
    image: trzl/portfolio:redeyephoto-web_v0.0.1
    container_name: redeyephoto-web
    restart: unless-stopped
    expose:
      - "80"
      - "443"
    environment:
      VIRTUAL_HOST: redeyephoto.tbrownkcdev.com
      LETSENCRYPT_HOST: redeyephoto.tbrownkcdev.com
      LETSENCRYPT_EMAIL: brownty47@gmail.com
networks:
  default:
    external:
      name: webproxy
